{
  "name": "@tokenbroke/shared",
  "license": "MIT",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./node/signing": "./src/node/signing.ts"
  },
  "scripts": {
    "build": "echo 'shared is consumed as TypeScript source by workspace packages'",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.node.json"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^3.2.0"
  }
}
