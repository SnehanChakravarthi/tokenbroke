# @tokenbroke/web

The tokenbroke.lol site: dual-tab leaderboard, misery meters, days-since-last-reset counters,
claim flow, OG card generator, and the API the CLI submits to.

Next.js App Router on Vercel. See the root `AGENTS.md` for product context and `AGENTS.md` in this
directory for Next.js-version-specific notes.

```bash
bun run dev   # from repo root, or `bun run dev` here
```
